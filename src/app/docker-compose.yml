version: "3.9"

services:
  saintvenant:
    build:
      context: .
      dockerfile: Dockerfile
    image: saintvenant:latest
    container_name: saintvenant_api
    ports:
      - "8000:8000"
    networks:
      - app_net
    restart: unless-stopped

networks:
  app_net:
    driver: bridge
